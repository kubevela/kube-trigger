watch:
  apiVersion: "v1"
  kind: "ConfigMap"
  namespace: vela-system
filters:
  - type: "cue-validator"
    properties:
      cue:
        template: |
          metadata: name: "cm-name"
To:
  name: "first-app"
  namespace: "default"