---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: kubetriggerconfigs.standard.oam.dev
spec:
  group: standard.oam.dev
  names:
    kind: KubeTriggerConfig
    listKind: KubeTriggerConfigList
    plural: kubetriggerconfigs
    singular: kubetriggerconfig
  scope: Namespaced
  versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          description: KubeTriggerConfig is the Schema for the kubetriggerconfigs API
          properties:
            apiVersion:
              description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
              type: string
            kind:
              description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
              type: string
            metadata:
              type: object
            spec:
              description: KubeTriggerConfigSpec defines the desired state of KubeTriggerConfig
              properties:
                selector:
                  additionalProperties:
                    type: string
                  description: 'INSERT ADDITIONAL SPEC FIELDS - desired state of cluster
                  Important: Run "make" to regenerate code after modifying this file'
                  type: object
                watchers:
                  description: Config for kube-trigger
                  items:
                    properties:
                      actions:
                        items:
                          properties:
                            properties:
                              type: object
                              x-kubernetes-preserve-unknown-fields: true
                            type:
                              type: string
                          required:
                            - properties
                            - type
                          type: object
                        type: array
                      filters:
                        items:
                          properties:
                            properties:
                              type: object
                              x-kubernetes-preserve-unknown-fields: true
                            type:
                              type: string
                          required:
                            - properties
                            - type
                          type: object
                        type: array
                      source:
                        properties:
                          properties:
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          type:
                            type: string
                        required:
                          - properties
                          - type
                        type: object
                    required:
                      - actions
                      - filters
                      - source
                    type: object
                  type: array
              required:
                - selector
              type: object
            status:
              description: KubeTriggerConfigStatus defines the observed state of KubeTriggerConfig
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: { }
